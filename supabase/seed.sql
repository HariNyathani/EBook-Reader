-- supabase/seed.sql
-- Phase 3: Optional local seed data.
-- No rows that assume users exist. Safe to run against a clean database.
-- Operator note: to create the first admin account, run:
--   pnpm bootstrap:admin <your-email>
-- after signing up normally via the app.

-- (No rows seeded in this phase — all content is user-generated.)
select 'seed.sql: no seed data in Phase 3 — use bootstrap:admin to promote first admin' as note;
