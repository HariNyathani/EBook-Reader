// Server Component — placeholder for the login page
// Phase 4 will implement: email+password auth form, Supabase sign-in Server Action, error display.
export default function LoginPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-bold">Sign In</h1>
      <p className="mt-2 text-sm text-gray-500">Login — Coming soon (Phase 4)</p>
    </main>
  );
}
