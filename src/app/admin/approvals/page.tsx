// Server Component — Admin user approval placeholder
// Phase 4 will implement: list of unapproved users, approve/reject Server Actions,
// using the admin (service-role) Supabase client to update profiles.is_approved.
export default function AdminApprovalsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">User Approvals</h1>
      <p className="mt-2 text-sm text-gray-500">Approval queue — Coming soon (Phase 4)</p>
    </div>
  );
}
