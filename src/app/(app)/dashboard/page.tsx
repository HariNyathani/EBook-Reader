// Server Component — Library / Dashboard placeholder
// Phase 5 will implement: book grid, library fetching, search/filter.
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-bold">Your Library</h1>
      <p className="mt-2 text-sm text-gray-500">Book grid — Coming soon (Phase 5)</p>
    </main>
  );
}
