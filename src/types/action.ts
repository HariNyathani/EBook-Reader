/**
 * Re-exports the ActionResult type from the canonical result module.
 * Import from '@/types' or '@/types/action' for use in Server Actions.
 */
export type { ActionResult } from '@/lib/result';
